<%= bootstrap_form_for(@sparql_endpoint) do |f| %>
  
  <% if @sparql_endpoint.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(@sparql_endpoint.errors.count, "error") %> prohibited this sparql_endpoint from being saved:</h4>

      <ul>
      <% @sparql_endpoint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, class:"form-control" %>
  </div>
  <div class="form-group">
    <%= f.text_field :endpoint, class:"form-control"%>
  </div>
  
  <div class="form-actions">
    <input name="commit" value="Submit" type="submit" class="btn btn-default btn-block btn-primary">
  </div>
<% end %>